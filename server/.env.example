# Orders are currently Cash-on-Delivery only — no payment gateway keys needed.
# When you're ready to add online payments back, this is where those keys will go.

# Port the API server runs on
PORT=4242

# The URL your React dev server / production site runs on (for CORS)
CLIENT_ORIGIN=http://localhost:5173
